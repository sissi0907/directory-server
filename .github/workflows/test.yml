name: Reproduce Flaky Tests

on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '8'
          
      # - name: maven clean install 
      #   run: mvn clean install -Dcheckstyle.skip -Drat.skip -U
        
      # - name: Compile Module
      #   run: mvn test-compile -am -Dcheckstyle.skip -Drat.skip -U
      # - name: checkout
      #   run: mvn -v
      - name: CHeck
        uses: actions/checkout@v3
        with:
          ref: f003088137d42461717590c763eb1f40b8d06a65
  
      - name: Test with Maven 
        run: | 
              for i in {1..5}; do mvn test -Dtest=org.apache.directory.server.core.partition.impl.btree.jdbm.JdbmIndexTest#testSingleValuedAttribute -fae; done#>>test_log.txt; done
              # echo "Maven tests completed." 
      # - name: Upload Test Log 
      #   if: always() 
      #   uses: actions/upload-artifact@v2 
      #   with: 
      #     name: test_log 
      #     path: test_log.txt
